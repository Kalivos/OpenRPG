OpenRPG is yet another open source text based role playing game. OpenRPG is written in PHP.

The project has just started. As such, the only portion under development is the database.

Priorities after database design:
Database scripts
Including stored procedures, indexes, views, triggers, etc.

Creation of config.php and base configuration
Creation of PHP classes to interface with database
Web Interface
